{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
<div class="block-center mt-4 wd-xl">
  <div class="card card-flat">
    <div class="card-header text-center bg-dark">
      <h1 style="color:white;">MedApp</h1>
    </div>

      <div class="card-body">
        <p class="text-center py-2">Ingrese al sistema</p>
        <form class="mb-3" id="loginForm" novalidate="novalidate">
          <div class="form-group">
            <div class="input-group with-focus">
              <input type="email" value="{{ last_username }}" name="email" id="inputEmail"  autofocus="autofocus"
                class="form-control border-right-0" placeholder="Email" required="required">
              <div class="input-group-append">
                <span class="input-group-text text-muted bg-transparent border-left-0">
                  <em class="fa fa-envelope"></em>
                </span>
              </div>
            </div>
          </div>

          <div class="form-group">
            <div class="input-group with-focus">
              <input type="password" name="password" id="inputPassword" class="form-control border-right-0"
                placeholder="Contraseña" required="required">
              <div class="input-group-append">
                <span class="input-group-text text-muted bg-transparent border-left-0">
                  <em class="fa fa-lock"></em>
                </span>
              </div>
            </div>
          </div>

          <div class="clearfix">
            <div class="checkbox c-checkbox float-left mt-0">
              <label>
                <input type="checkbox" value="" name="remember">
                <span class="fa fa-check"></span>
                Recordarme
              </label>
            </div>
            <div class="float-right">
              <a class="text-muted" href="recover.html">Olvide su clave?</a>
            </div>
          </div>

          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
          <button class="btn btn-block btn-primary mt-3" type="submit">Ingresar</button>

        </form>

        <p class="pt-3 text-center">Aún no tiene usuario?</p>
        <a class="btn btn-block btn-secondary" href="{{ path('doctor_registration') }}">Registrarse</a>
      </div>
  </div>
</div>
{% endblock %}